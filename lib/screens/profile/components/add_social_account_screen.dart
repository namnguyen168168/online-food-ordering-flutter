import 'package:flutter/material.dart';

class AddSocialAccountScreen extends StatelessWidget {
  const AddSocialAccountScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Add Social Account"),
      ),
      body: Center(
        child: Text("Add Social Account Screen"),
      ),
    );
  }
}