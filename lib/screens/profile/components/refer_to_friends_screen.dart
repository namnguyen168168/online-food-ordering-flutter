import 'package:flutter/material.dart';

class ReferToFriendsScreen extends StatelessWidget {
  const ReferToFriendsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Refer to Friends"),
      ),
      body: Center(
        child: Text("Refer to Friends Screen"),
      ),
    );
  }
}